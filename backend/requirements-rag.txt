# BVC24 ERP — Enterprise RAG AI Platform dependencies.
# Install with:  pip install -r requirements-rag.txt
#
# Heavy/optional add-on, kept separate from requirements.txt (same pattern
# as requirements-ai.txt) so the core app stays light to install.
#
# Requires a Qdrant instance reachable at QDRANT_URL (see
# deploy/nssm-install-qdrant.ps1 to run it as a local Windows service).
#
# Embedding model (BAAI/bge-small-en-v1.5) downloads on first use (~130 MB)
# and is cached under the user's home directory (~/.cache/huggingface).

--extra-index-url https://download.pytorch.org/whl/cpu

qdrant-client==1.12.1
sentence-transformers==3.3.1
torch==2.5.1
pypdf==5.1.0
