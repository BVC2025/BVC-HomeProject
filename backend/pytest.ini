[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    smoke: lightweight reachability tests
    integration: end-to-end flow tests
    security: auth + input validation tests
